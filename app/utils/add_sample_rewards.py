import logging
from app.utils.logger import logger

def add_sample_rewards():
    """This function is now integrated into init_database()"""
    logger.info("ℹ️ Sample rewards are now added during database initialization")
    
if __name__ == "__main__":
    add_sample_rewards()
